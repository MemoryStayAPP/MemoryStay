// const Center = ({markers}) => {
//     const map = useMap();
//     const { uid } = useParams();
//     useEffect(() => {
//     markers.forEach(marker => marker.uuid === uid ? map.setView([marker.lat, marker.lng]) : null);
//       map.setView(center);
//     }, [center, map]);
  
//     return null;
//   };
